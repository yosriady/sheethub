<%
title "Contact"
description "SheetHub Contact"
keywords %w(contact support)
%>

<div class="animated-banner">
    <h1>Contact Us</h1>
    <h2>Have questions? We're happy to help!</h2>
</div>

<div class="container">
  <div class="row">

    <div class="col-xs-8 white-container">
        <%= simple_form_for(@contact, html: { class:'sleek-form' }, defaults: { class:'big-form', input_html: { class: 'form-control' }}) do |f| %>
          <%= f.input :name, label: 'Your Name', required: true %>
          <%= f.input :email, required: true %>
          <%= f.input :subject, required: true %>
          <%= f.input :message, as: :text, required: true %>

          <%= f.button :submit, 'Send', class: "btn btn-primary btn-lg" %>
        <% end %>
    </div>
    <div class="col-xs-4">
        <div class="bg-info blurb"><strong><em class="fa fa-info-circle"></em> Alpha</strong> <p>We are currently in public alpha, which means things may not work perfectly – yet. If you encounter something that looks broken, please let us know!</p></div>
        <hr>
        <div class="panel panel-default">
            <%= render 'layouts/twitter_widget' %>
            <%= render 'layouts/facebook_widget' %>
        </div>

    </div>
  </div>
</div>
