<%
title "Upgrade"
keywords %w(upgrade pro plus premium)
%>


<div class="container white-container" id="upgrade-page">
    <div class="animated-banner">
        <h1 style="color: #5cb85c; font-weight:bold;">Go Pro on SheetHub</h1>
        <h2>Unlimited free uploads, private links, and more.</h2>
    </div>

    <% if current_user %>
    <p class="bg-info"><i class="fa fa-info-circle"></i> You are currently on the <em><%= current_user.membership_type.titleize %></em> plan. <% unless (current_user && (current_user.membership_type == "plus" || current_user.membership_type == "pro")) %><a href="<%= subscriptions_purchase_path(:pro) %>">Upgrade</a> now to Plus or Pro to enjoy unlimited free uploads and extra features.<% end %></p>
    <% end %>

    <div class="col-xs-4">
    <%= render 'subscriptions/basic_table' %>
    </div>
    <div class="col-xs-4">
        <%= render 'subscriptions/plus_table', nobutton:false %>
    </div>
    <div class="col-xs-4">
      <%= render 'subscriptions/pro_table', nobutton:false %>
    </div>


</div>