<%
title "Upgrade"
keywords %w(upgrade pro plus premium)
%>


<div class="container white-container" id="upgrade-page">
    <div class="animated-banner">
        <h1 style="color: #5cb85c; font-weight:bold;">Go Pro on SheetHub</h1>
        <h2>Unlimited free uploads and more.</h2>
    </div>

    <% if current_user %>
    <p class="bg-info"><em class="fa fa-info-circle"></em> As a <%= current_user.membership_type.titleize %> member, you already have access to many features. <% if current_user.basic? %> You can <a href="<%= subscriptions_purchase_path(:pro) %>">Upgrade</a> to enjoy unlimited free uploads and even more features!<% end %></p>
    <% end %>

    <div class="col-xs-4">
    <%= render 'subscriptions/basic_table' %>
    </div>
    <div class="col-xs-4">
        <%= render 'subscriptions/plus_table', nobutton:false %>
    </div>
    <div class="col-xs-4">
      <%= render 'subscriptions/pro_table', nobutton:false %>
    </div>


</div>