<% for tag in tags %>
    <a href="<%= tag_path(tag) %>">
    <span class="tag">
        <%= tag.name.titleize %>
        <span class="tag-count"><%= tag.taggings_count %></span>
    </span>
    </a>
<% end %>