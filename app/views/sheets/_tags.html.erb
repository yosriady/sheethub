<% for tag in tags %>
    <a href="<%= genre_path(tag.name) %>">
    <span class="tag">
        <%= tag.name.titleize %>
        <span class="tag-count"><%= tag.taggings_count %></span>
    </span>
    </a>
<% end %>