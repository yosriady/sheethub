<%
title "Browse"
%>

<%= render 'layouts/nav_strip' %>

<div class="container">

<%= render 'browse_sidebar', instruments: @instruments, genres: @genres, composers: @composers, sources: @sources %>

<div class="col-xs-10">
  <div class="row">
    <div class="col-lg-12">
        <%= render 'sheets', sheets: @sheets %>
    </div>
  </div>
</div>
</div>
