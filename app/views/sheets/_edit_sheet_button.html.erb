<% if @current_user.present? && current_user == @sheet.user %>
<a href="<%= edit_sheet_path(@sheet) %>">
    <button type="button" class="btn btn-success btn-lg btn-block"><span class="glyphicon glyphicon-edit"></span> Edit</button>
</a>
<% end %>