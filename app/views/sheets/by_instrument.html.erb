<% title "Discover by Instrument | #{params[:slug].titleize}" %>

<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <%= render 'tag_breadcrumb', tag:params[:slug] ,tag_type:"instrument" %>
      <%= render 'sheets', sheets: @sheets %>
    </div>
  </div>
</div>
