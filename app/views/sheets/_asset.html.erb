<div class="asset">
  <% new_or_existing = asset.new_record? ? 'new' : 'existing' %>
  <% prefix = "page[#{new_or_existing}_asset_attributes][]" %>

  <% fields_for prefix, asset do |asset_form| -%>
    <p>
      Asset: <%= asset_form.file_field :file %>
      <!-- TODO: link to remove this asset -->
    </p>
<% end -%>
</div>