<div id="sheet-info" class="panel panel-info">
  <div class="panel-body" style="padding-bottom: 5px">

    <% if sheet.description? %>
        <p><%= sheet.description_html %></p>
    <% end %>

    <% if sheet.license.present? %>
    <strong class="publishing-rights"><em class="fa fa-certificate"></em> <%= sheet.verbose_license %> <a href="<%= support_url %>#licenses"><em class="fa fa-question-circle"></em></a></strong>
    <% end %>

    <p class="fade-text"><span class="fa fa-book"></span> <%= pluralize(sheet.pages, 'page') %></p>
    <p data-toggle="tooltip" data-placement="left" title="Uploaded <%= sheet.created_at.strftime('%d %B %Y') %>"><span class="fade-text hover-pointer"><span class="fa fa-clock-o"></span> Uploaded <%= distance_of_time_in_words(Time.zone.now, sheet.created_at) %> ago</span></p>

    <% if sheet.cached_publishers.present? %>
    <p class="fade-text"><span class="fa fa-copyright"></span> <%= sheet.cached_publishers %></p>
    <% end %>
  </div>

    <ul class="list-group">
        <li class="list-group-item flat-tags well">
            <%= render 'sheet_tags', sheet:sheet %>
        </li>
    </ul>
</div>
