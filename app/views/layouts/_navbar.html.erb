<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_url %>" >SheetHub</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="<%= browse_url %>"><em class="fa fa-picture-o"></em> Browse</a></li>
        <li><a href="<%= sheet_upload_url %>"><em class="fa fa-cloud-upload"></em> Upload</a></li>

      <% if user_signed_in? %>
        <%= render 'layouts/user_dropdown' %>
      <% else %>
        <li><a href="<%= new_user_session_url %>">Sign In</a></li>
        <li><a href="<%= new_user_registration_url %>">Sign Up</a></li>
      <% end %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><em class="fa fa-bars"></em></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="<%= faq_url %>"><em class="fa fa-question-circle"></em> Help</a></li>
            <li><a href="<%= community_guidelines_url %>"><em class="fa fa-smile-o"></em> Guidelines</a></li>
            <li><%= link_to terms_url do %><em class="fa fa-legal"></em> Terms of Service<% end %></li>
          </ul>
        </li>
      </ul>
      <%= render 'layouts/searchbar' %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>