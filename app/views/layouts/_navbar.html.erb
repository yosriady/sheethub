<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">SheetHub</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:action] == 'index' %>"><a href="<%= root_path =%>"><i class="fa fa-picture-o"></i> Discover</a></li>
        <li class="<%= 'active' if params[:action] == 'new' %>"><a href="<%= new_sheet_path =%>"><i class="fa fa-cloud-upload"></i> Upload</a></li>

      <% if user_signed_in? %>
        <%= render 'layouts/user_dropdown' %>
      <% else %>
        <li><a href="<%= new_user_session_path =%>"><i class="fa fa-user"></i> Sign In</a></li>
      <% end %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-bars"></i></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="<%= faq_path %>"><i class="fa fa-question-circle"></i> FAQ</a></li>
            <hr>
            <li><%= link_to terms_path do %><i class="fa fa-info-circle"></i> Terms<% end %></li>
            <li><%= link_to privacy_path do %><i class="fa fa-eye"></i> Privacy<% end %></li>
          </ul>
        </li>
      </ul>
      <%= render 'layouts/searchbar' %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>