<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade in">
    <span class="glyphicon glyphicon-<%= glyphicon_for(type) %>"></span>
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>