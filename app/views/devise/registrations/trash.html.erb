<% title "Trash" %>

<% if current_user %>
  <%= render 'profile_banner', user:current_user %>

  <div class="container">
    <div class="row">
    <div class="col-xs-12">
        <h2>Your Deleted Sheets</h2>
        <%= render 'deleted_sheets_tab_content' %>
    </div>
  </div>
<% else %>
  <h2>You need to be logged in</h2>
<% end %>
  </div>