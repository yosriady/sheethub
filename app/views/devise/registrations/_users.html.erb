<% users.each do |user| %>
  <%= render 'devise/registrations/user_detailed', user: user %>
<% end %>

<div class="row">
    <div id="paginator" class="text-center">
      <%= paginate users %>
    </div>
</div>