<% if @user %>
  <% title "#{@user.username}'s Profile"  %>

  <%= render 'profile_banner', user:@user %>
  <div class="nav-strip">
  <div class="container-fluid">
  <ul class="list-inline">
    <li><a href="#sheets"><i class="fa fa-music"></i> Sheet Music <span class="badge"><%= @sheets.count %></span></a></li>
    <li><a href="<%= user_favorites_path(@user.username) %>"><i class="fa fa-star"></i> Favorites </a></li>
  </ul>
  </div>
</div>

  <div class="container">
      <div class="row">
        <div class="col-xs-12">

          <!-- Nav tabs -->
          <ul class="nav nav-pills">
            <li class="active"><a href="#sheets"><i class="fa fa-music"></i> Sheet Music <span class="badge"><%= @sheets.count %></span></a></li>

            <li><a href="<%= user_favorites_path(@user.username) %>"><i class="fa fa-star"></i> Favorites </a></li>
          </ul>

          <!-- Tab panes -->
          <div class="tab-content">
            <%= render 'sheets_tab_content', sheets: @sheets, user: @user %>
          </div>


        </div>
    </div>
  <% else %>
    <% title "User not found" %>
    <h2>User not found</h2>
  <% end %>

  </div>