<%
title "#{@user.display_name}"
description @user.tagline
%>

<%= render 'profile_banner', user:@user %>
<div class="nav-strip nav-strip-profile">
<div class="container-fluid">
<ul class="list-inline">

  <li class="active"><a href="<%= user_profile_url(subdomain: @user.username) %>">Works <span class="badge"><%= @sheets.count %></span></a></li>
  <li><a href="<%= user_favorites_url(subdomain: @user.username) %>"> Favorites </a></li>

</ul>
</div>
</div>

<div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="tab-content">
          <%= render 'sheets_tab_content', sheets: @sheets, user: @user %>
        </div>
      </div>
  </div>
</div>