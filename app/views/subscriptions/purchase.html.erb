<% title "Upgrade to #{params[:membership].titleize}" %>

<div class="container">
    <div class="row">
        <h1>Upgrade to <%= params[:membership].titleize %> today</h1>
        <div class="col-xs-8 white-container">
            <p class="bg-info"><i class="fa fa-thumbs-up"></i> You are now one step away to a SheetHub <%= params[:membership].titleize %> membership</p>

            <hr>
            <strong>What happens next?</strong>
            <p>You'll be redirected to Paypal to confirm your payment details of <%= number_to_currency(Subscription.subscription_amount(params[:membership])) %> per month. Once you've confirmed the purchase, your new SheetHub <%= params[:membership].titleize %> membership will activate effective immediately along with membership perks. Enjoy your unlimited uploads and awesome new features! </p>
            <hr>

            <%= image_tag("https://www.paypalobjects.com/webstatic/en_SG/mktg/Logos/AM_SbyPP_mc_vs_dc_ae.jpg", class:"paypal-logo") %>
            <a id="paypal-checkout-btn" href="<%= subscriptions_checkout_path(params[:membership]) %>" data-method="post"><button type="button" class="btn btn-lg btn-success pull-right"><i class="fa fa-paypal"></i> Checkout with PayPal</button></a>
        </div>
        <div class="col-xs-4">
            <p class="bg-info"><i class="fa fa-star"></i> You will enjoy all <%= params[:membership].titleize %> membership benefits: </p>
            <%= render 'subscriptions/pro_table', nobutton: true if params[:membership] == 'pro' %>
            <%= render 'subscriptions/plus_table', nobutton: true if params[:membership] == 'plus' %>
        </div>
    </div>
</div>
